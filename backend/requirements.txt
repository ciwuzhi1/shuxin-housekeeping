# 舒心家政服务平台 · 后端依赖（Python 3.12）
# 安装：backend/.venv/Scripts/python.exe -m pip install -r backend/requirements.txt
# 版本与 backend/.venv 实测环境一致（2026-08-29）。

# Web 框架
fastapi==0.141.1
# ASGI 服务器（standard 附带 websockets/watchfiles 等加速组件）
uvicorn[standard]==0.52.1

# MySQL 驱动（纯 Python 实现）
PyMySQL==1.2.0
# PyMySQL 认证所需的加密支持（caching_sha2_password 等）
cryptography==50.0.0
# 数据库连接池
DBUtils==3.1.2

# 认证
# JWT 签发与校验（HS256）
PyJWT==2.13.0
# 密码慢哈希 Argon2id（v4.0 起替代 SHA-256+固定盐，旧哈希登录时自动升级）
argon2-cffi==25.1.0

# 配置
# 可选加载根目录 .env 环境变量文件
python-dotenv==1.2.2

# 测试
pytest==9.1.1
# e2e 测试的 HTTP 客户端
httpx==0.28.1
# 覆盖率报告（CI 门禁 --cov-fail-under）
pytest-cov==7.1.0

# 质量门禁（CI 执行，本地可用）
# lint：pyflakes/pycodestyle 错误类 + 导入排序（规则集见 pyproject.toml）
ruff==0.16.5
# Python 依赖漏洞审计（对照 requirements.txt 固定版本）
pip-audit==2.10.1
